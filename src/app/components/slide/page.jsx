import React from 'react';
/*
// Functional component
const Slide = ({ img, title, price }) => {
  return (
    <div className="outline-none border-none relative">
      <img src={img} alt={title} />
      <h3>{title}</h3>
      <p>{price}</p>

      <div className="absolute left-[30px] md:left-[70px] max-w-[250px] sm:max-w-[350px] top-[50%]-translate-y-[50%] space-y-2 lg:space-y-4 bg-[#ffffffa2 sm:bg-transparent p-4 sm:p-0 rounded-lg sm:rouned-none">

      </div>
    </div>
  );
};

export default Slide; */
